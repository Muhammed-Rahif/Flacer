import 'package:flutter/material.dart';

class HelpersPage extends StatelessWidget {
  const HelpersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Helpers Page');
  }
}
